const users = require('./userfixtures')
const sellers = require('./sellerfixtures')
const products = require('./productfixture')
const orders = require('./orderfixture')

module.exports = {
    users,
    sellers,
    products,
    orders
}
